#include <bits/stdc++.h>
using namespace std;
int main(){
    const int ci=1024;
    double dval = 3.14;
    const int temp = dval;
    // int &ri = temp; error: cannot bind non-const lvalue reference of type 'int&' to an rvalue of type 'int'
    return 0;
}
// Use this template always on leetcode to improve runtime.
static const int _ = []() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
   return 0;
}();

